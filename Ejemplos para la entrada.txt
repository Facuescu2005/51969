Ejemplo 1: (Entrada válida)
x = 10;
y = 5;
z = x + y;
console.log(z);
switch(x) {
    case 10:
        y = y + 1;
        console.log(y);
    case 15:
        y = y + 2;
        console.log(y);
    default:
        z = z + 1;
        console.log(z);
}


Ejemplo 2: (Entrada válida)
nombre = vJuanv;
console.log(nombre);
switch (5) {
    case 5:
        mensaje = vHolamundov;
        console.log(mensaje);
    default:
        console.log(vAdiosv);
}


Ejemplo 3: (Entrada inválida)
a = 10;
b = a % 3;


Esta entrada es inválida, ya que la gramática solo define +, -, *, /.


Ejemplo 4:(Entrada inválida)
texto = vHola mundo con espaciosv;
 
Esta entrada es inválida, ya que la gramática no permite espacios dentro de los strings delimitados por v.
